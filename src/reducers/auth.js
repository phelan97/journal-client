import {
  
} from './actions/auth';

const initialState = {

};

export default function authReducer(state=initialState, action) {
  switch(action.type) {
    case 
    default:
      return state;
  }
}